from pyDatalog import pyDatalog
pyDatalog.clear()
pyDatalog.clear()
pyDatalog.clear()

class Person(pyDatalog.metaMixin):

    def __init__(self):,name,age,glycemie):
        super(Person, self).__init__():